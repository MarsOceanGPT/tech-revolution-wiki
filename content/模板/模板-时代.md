---
序号: X
时间跨度: "YYYY-YYYY"
核心范式: ""
制度分组: ""
上一时代: "[[时代X：XXX]]"
下一时代: "[[时代X：XXX]]"
---

# 时代 X：名称（YYYY-YYYY）

> 核心范式一句话

## 时间线

| 年份 | 事件 | 链接 |
|---|---|---|

## 关键人物

```dataview
LIST
FROM "人物"
WHERE contains(关联时代, this.file.link)
```

## 案例

### [[维度：A-范式滞后]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：A-范式滞后]])
SORT 年份
```

### [[维度：B-基础设施]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：B-基础设施]])
SORT 年份
```

### [[维度：C-金融狂热]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：C-金融狂热]])
SORT 年份
```

### [[维度：D-制度滞后]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：D-制度滞后]])
SORT 年份
```

### [[维度：E-技能断裂]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：E-技能断裂]])
SORT 年份
```

### [[维度：F-文化冲击]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：F-文化冲击]])
SORT 年份
```

### [[维度：G-路径依赖]]
```dataview
LIST
FROM "案例"
WHERE contains(时代, this.file.link) AND contains(维度, [[维度：G-路径依赖]])
SORT 年份
```

## 死胡同：失败的替代路线

```dataview
LIST
FROM "死胡同"
WHERE contains(时代, this.file.link)
```

## 这个时代的核心教训

[待填充]

## 与其他时代的关键类比

[待填充——标注类比的成立维度和边界]
